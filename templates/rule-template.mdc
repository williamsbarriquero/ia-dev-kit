---
description: "<Quando esta regra ativa — ex.: ao editar arquivos .java>"
globs: ["**/*.ext"]
alwaysApply: false
---
# Padrões <Stack> RPE

## Ativação

Ao editar arquivos matching o glob, leia a skill **`<stack>-mastery`** — conteúdo canônico em `.cursor/knowledge/stacks/<stack>.md`.

## Verificação

<!-- Apenas comandos; não duplicar conteúdo técnico. Preferir referência a stack-baseline.mdc -->

- `<comando lint>`
- `<comando test>`
